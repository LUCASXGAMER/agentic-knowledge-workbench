# 本地验证记录

验证日期：2026-09-24。测试在为公开副本新建的 Python 3.14.7 环境中执行，前端通过 `npm ci --ignore-scripts` 安装锁文件中的依赖。没有使用原项目数据库或真实样本。

| 检查 | 结果 |
| --- | --- |
| 后端测试 | 14 项通过，0 失败 |
| 前端构建 | `npm run build` 通过 |
| 页面打开检查 | 18 个路由均有内容，0 个 JavaScript 未捕获异常 |

## 实际流程

- 从空数据库导入 3 篇合成文档，实际完成提问、检索和引用展示。
- 3 个有预期文档的合成问题均命中，另 1 个拒答问题未纳入此检索指标。这个规模仅说明演示链路可运行，不能用作性能宣传。
- 网页上实际点击运行检索评测，结果来自后端当前索引。

## 复查命令

```bash
PYTHONPATH=backend python -m pytest backend/tests -q
cd frontend
npm run build
```

## 验证边界

页面打开检查覆盖加载与异常，不表示每个页面的每个交互都已测试。真实外部模型、生产负载、多租户部署和独立业务效果未验证。GitHub Actions 工作流已配置，本次没有触发云端运行。当前工具链有弃用提示，未导致测试或构建失败。
